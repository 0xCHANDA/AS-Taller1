---
description: Audit C# architecture and dependency direction
agent: architecture-auditor
subtask: true
---

Audit `$ARGUMENTS` for component responsibilities, cohesion/coupling, layers, MVC boundaries, Clean Architecture dependency direction and ports/adapters where relevant. Do not impose a pattern without requirements.
