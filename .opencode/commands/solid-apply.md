---
description: Apply an explicitly approved SOLID refactoring scope
agent: refactor-implementer
subtask: true
---


Implement only the approved refactoring scope in `$ARGUMENTS`. Verify the baseline first, protect behavior, apply one slice at a time, build/test each slice and stop on unexplained regression or scope expansion. Report every file changed and command result.
