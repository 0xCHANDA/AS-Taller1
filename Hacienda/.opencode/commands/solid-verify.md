---
description: Verifica el refactor y ejecuta revisión adversarial
agent: solid-orchestrator
---

Verifica `$ARGUMENTS`: delega pruebas a `test-guardian` y revisión independiente a `adversarial-reviewer`; compara con baseline y no corrijas automáticamente.
